__pycache__/
*.pyc

.claude/

# HTTP response cache — large, regenerable from Wayback + Open-Meteo.
# Generated summary CSVs are kept so the repo is useful without a rerun.
cache/https___*
cache/http___*
cache/web.archive.org_*
cache/live_foreca_*.html

# Large regenerable ML prediction frames (keep only summary CSVs)
cache/ml_standalone_predictions.csv
cache/ml_mos_predictions.csv
cache/ml_vs_foreca_subset.csv
